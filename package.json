{
  "name": "fwdslsh-website",
  "version": "0.0.2",
  "description": "The official website for fwdslsh",
  "type": "module",
  "homepage": "https://fwdslsh.dev",
  "scripts": {
    "build": "unify build --source src --output dist --pretty-urls --base-url https://fwdslsh.dev/ --canonical auto",
    "dev": "unify dev --source src",
    "check": "unify build --source src --output dist --pretty-urls --base-url https://fwdslsh.dev/ --canonical auto --dry-run --strict",
    "audit": "unify audit --source src --output dist --pretty-urls --base-url https://fwdslsh.dev/ --canonical auto --strict",
    "test": "playwright test"
  },
  "devDependencies": {
    "@fwdslsh/unify": "^0.8.2",
    "@playwright/test": "^1.57.0"
  },
  "dependencies": {
    "@speed-highlight/core": "^1.2.14"
  }
}
